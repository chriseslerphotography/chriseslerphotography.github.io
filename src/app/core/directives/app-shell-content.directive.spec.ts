import { AppShellContentDirective } from './app-shell-content.directive';

describe('AppShellContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShellContentDirective();
    expect(directive).toBeTruthy();
  });
});
